# Secrets

Plain Kubernetes Secret payloads are prohibited. GitOps may declare ExternalSecret or CSI references to externally managed secret values. Production workload identity is the default cloud authentication model.
